class Lactancy extends Mom
{
    constructor(rightBreast, leftBreast, lastFood, nextFood)
    {
        super( rightBreast, leftBreast);
        this.lastFood = lastFood;
        this.nextFood = nextFood; 

    }
    chrono(e)
    {
    
    }
}